## Status: ISSUES_FOUND

> AC#6 has no standalone verify item and is absent from all `implements` lists; all other FRs and ACs are formally covered.

---

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T03 | Each exchange of an active-leaf branch is embedded as its own `chunk_vec` vector via `embed_branch_chunks` — verified by chunk/vector counts in `tests/test_session_ops.py` |
| FR#2 | T06 | A query matching any single exchange surfaces its session, including exchanges outside the old summary window (AC#1) |
| FR#3 | T06 | Entrypoint A ranks sessions by best-matching chunk (max rollup) composed with FTS via the unchanged RRF (AC#2) |
| FR#4 | T07 | `ccrecall search-messages` returns matched exchanges directly (not rolled up to session), semantically ranked by chunk distance |
| FR#5 | T03 | Steady-state embedding is bounded by new/content-changed exchanges — appending one exchange embeds exactly one new chunk; an unchanged re-sync embeds nothing (AC#4) |
| FR#6 | T04 | Bumping `EMBEDDING_VERSION` makes the entire active-leaf corpus eligible; a backfill run re-embeds it at chunk granularity (AC#5) |
| FR#7 | T01 | Deleting a `branches` row removes all its `chunks` rows and all their `chunk_vec` rows (two-level cascade), verified by count in `tests/test_db.py` (note: parenthetically calls this AC#6, but no standalone `- [ ] AC#6:` line exists — see Coverage Gaps) |
| FR#8 | T01 | Schema additions are additive (`CREATE ... IF NOT EXISTS`) and touch no row in `messages`/`branches`/`branch_messages` |
| FR#8 | T06 | `branch_vec` is dropped losslessly (derived data); `upsert_branch_vec`/`write_branch_embedding` removed; `messages`/`branches`/`branch_messages` are untouched; `test_legacy_migration.py` confirms migration preserves history |
| FR#9 | T06 | Stale (previous-version / wrong-model) chunk vectors are excluded at the chunk grain in the query, so a partially re-embedded branch still returns its current chunks (AC#8) |
| FR#10 | T02 | `cap_for_embedding` produces a head+tail-capped form and tightens until `len(tokens) <= MODEL_TOKEN_LIMIT`, yielding a usable vector rather than discarding the exchange — covered by `tests/test_embeddings.py` (AC#9 cap mechanism) |
| FR#11 | T05 | The card, snippet, and envelope renderers emit exactly the fields and markdown/JSON forms defined in `output-format-contract.md`, including normalized `score` / higher-is-better `score_raw` and the `ranked:false` unranked shape |
| FR#11 | T06 | A's card + envelope JSON/markdown conform to `output-format-contract.md` (no extra output fields), via the T05 renderers |
| FR#11 | T07 | B's snippet + envelope JSON/markdown conform to `output-format-contract.md` (no extra output fields), via the T05 renderers (AC#10 snippet half) |
| FR#11 | T09 | `tool-reference.md` and `SKILL.md` document card (A), snippet (B), and envelope shapes exactly as `output-format-contract.md` defines them, including `search-messages`, `score`/`ranked` semantics, `--verbose`/`--status`; `CLAUDE.md` describes `chunks`/`chunk_vec` schema |
| FR#12 | T06 | Entrypoint A emits one scored card per session (deduped to best-ranked branch) with no full transcript in the list (AC#2) |
| FR#13 | T07 | Each B result carries a `(handle, exchange_index, timestamp)` locator and a per-message-bounded excerpt (separate `user`/`assistant` fields) |
| FR#14 | T03 | The write path embeds only new/content-changed exchanges and is bounded per sync at `MAX_WRITE_PATH_EMBEDS_PER_SYNC`; version-stale chunks are left for backfill |
| FR#15 | T04 | The backfill re-selects any branch with a `chunks` row lacking a `chunk_vec` (crash victims, post-drop orphans) independent of the watermark (AC#12) |
| FR#16 | T08 | A second `sync-current` invocation while one is running skips (exits 0 without embedding) rather than running a parallel embed; the lock is reaped if stale (AC#13) |
| FR#17 | T07 | With the vector index unavailable, B returns a well-formed empty `ranked:false` envelope (never an error) — AC#14 |
| AC#1 | T06 | A query whose only match is a middle exchange of a >8-exchange session returns that session in Entrypoint A results |
| AC#2 | T06 | Entrypoint A returns one scored card per session, ranked by best-chunk fusion, with no full transcript in the list |
| AC#3 | T07 | B returns matched exchanges with a `(handle, exchange_index, timestamp)` locator and bounded `user`/`assistant` excerpts, ordered by chunk distance; not rolled up to session |
| AC#4 | T03 | After appending one exchange and re-syncing, exactly one new chunk vector is written and no existing chunk is re-embedded |
| AC#5 | T04 | After an `EMBEDDING_VERSION` bump, all active-leaf branches are eligible; post-backfill, every active-leaf exchange has a current-version chunk vector |
| AC#6 | — | **MISSING** — no `- [ ] AC#6:` verify item in any task; not in any `implements` list. T01's FR#7 verify text parenthetically notes "(this is AC#6)" but that does not constitute a formal verify item |
| AC#7 | T04 | `messages`/`branches`/`branch_messages` row counts and contents are unchanged across a version bump + backfill (integration-style, seeded DB) |
| AC#8 | T06 | A branch with mixed current/stale chunk vectors returns its current-version chunks and omits the stale ones |
| AC#9 | T02 | An exchange longer than the cap is embedded from a head+tail form and still produces a usable 512-dim vector, verified in `tests/test_embeddings.py` |
| AC#10 | T05 | Card and snippet JSON/markdown match `output-format-contract.md` field-for-field (renderer half) — single-result `score:null` and unranked-path cases included |
| AC#10 | T07 | B snippet JSON/markdown match `output-format-contract.md` field-for-field, including `matched_role:null`/`match_terms:[]` |
| AC#11 | T03 | After simulating an `EMBEDDING_VERSION` bump, the write path embeds at most `MAX_WRITE_PATH_EMBEDS_PER_SYNC` chunks and leaves the rest version-stale for backfill |
| AC#12 | T04 | A branch with a `chunks` row whose `chunk_vec` is missing is re-selected by backfill even when its watermark reads `EMBEDDING_VERSION` |
| AC#13 | T08 | A second `sync-current` started while one is running exits without embedding; the first completes normally — verified in `tests/test_sync_hook.py` with a held/fake lock file |
| AC#14 | T07 | With the vector index unavailable, `search-messages` exits 0 with an empty `ranked:false` envelope |
| AC#15 | T04 | A branch with `context_summary = NULL` is chunk-embedded (via `CHUNK_EMBEDDABLE_BRANCH_FILTER`) and searchable |

---

## Coverage Gaps

**AC#6** — Not present in any task's `implements` list, and no standalone `- [ ] AC#6:` verify item exists in any task's Verify section.

Design doc text: "AC#6 Deleting a branch row deletes all its `chunks` rows and all their `chunk_vec` rows (verified by count). (FR#7)"

T01's FR#7 verify item reads: "Deleting a `branches` row removes all its `chunks` rows and all their `chunk_vec` rows (the two-level cascade), verified by count in `tests/test_db.py` (this is AC#6)." The parenthetical "(this is AC#6)" makes the planner's intent explicit and the substance is not missing — T01's FR#7 test IS AC#6's test. However, the formal traceability is incomplete: AC#6 has no `- [ ] AC#6:` line and is absent from `implements`.

**Fix required:** Add `- [ ] AC#6: Deleting a branch row deletes all its chunks rows and all their chunk_vec rows, verified by count in tests/test_db.py.` to T01's Verify section. Optionally add `"AC#6"` to T01's `implements` list for completeness.

---

## Contradictions

None.

---

## Warnings

**W1 — ACs in `implements` lists (unusual but not invalid):** T04 lists `"AC#7"` in its `implements` array alongside FRs, and T05 lists `"AC#10"`. Both AC#7 and AC#10 are valid design doc identifiers. The convention is to put FRs in `implements` and verify ACs in the Verify section; having ACs in `implements` is inconsistent with the pattern used by T01–T03, T06–T09. This is not a traceability error but is worth normalizing for consistency.

**W2 — `context.md` Visual Artifacts says "None"; confirmed correct.** The design has no mockup or screenshot paths. No visual artifact verify items are expected or missing.

**W3 — T09 verify criterion is relatively coarse:** The single verify item for T09 is: "tool-reference.md and SKILL.md document the card (A), snippet (B), and envelope shapes exactly as output-format-contract.md defines them..." This is documentation-facing and harder to mechanically verify than code tests, but the criterion names specific artifacts (field names, parity, `search-messages` command, `--verbose`/`--status` semantics, `CLAUDE.md` schema update), which is sufficient for a docs task.

**W4 — context.md is complete and well-formed.** All five required sections are present and non-empty: Problem & Motivation ✓, Visual Artifacts ✓ (valid "None" present), Key Decisions ✓, Constraints & Anti-Patterns ✓, Design Doc References ✓. The optional sixth section (Convention Examples) is present with code snippets ✓.

**W5 — No identifier format violations.** All FR and AC identifiers in design.md match `^FR#\d+$` and `^AC#\d+$` respectively. All identifiers referenced in task files are valid design doc identifiers.
