## Status: APPROVED

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|---|---|---|
| FR#1 | T04 | `get_db_connection` initializes a fresh DB by applying `SCHEMA_CORE` + detected FTS with no migration call |
| FR#2 | T02 | Fresh DB built from `SCHEMA` alone has the three embedding columns (last three) and `idx_branches_embedding_version` |
| FR#3 | T04 | A fresh conversation DB has no `token_snapshots` table |
| FR#4 | T04 | `token_schema.ensure_schema` still creates `token_snapshots` on the token DB; token-DB tests pass; `token_schema.py` unchanged |
| FR#5 | T04 | No `PRAGMA user_version` read or write remains in `src/` (grep clean) |
| FR#6 | T04 | Opening an existing v6 DB via `get_db_connection` succeeds and leaves its rows/columns intact (AC#4 test passes) |
| AC#1 | T01 | Schema-snapshot test captures tables (excluding `token_snapshots`), per-table `table_info`, and `idx_*` indexes from a `get_db_connection`-built fresh DB and passes on current code |
| AC#2 | T02 | `tests/test_db.py` test asserts the three embedding columns are the last three `PRAGMA table_info(branches)` rows in order and `idx_branches_embedding_version` exists, on a schema-only DB; T01 pin still passes |
| AC#3 | T04 | `test_db.py` test asserts a fresh conversation DB has no `token_snapshots`, and `ensure_schema` still creates it on a token DB |
| AC#4 | T04 | `test_db.py` test opens a populated v6-style DB and confirms rows in `branches`/`messages`/`token_snapshots` are intact |
| AC#5 | T04 | `migrations.py` no longer exists and `grep -rn "ccrecall.migrations" src/ tests/` returns nothing |
| AC#6 | T03, T04 | T03: no test references migrations and full suite passes (with `migrations.py` still present); T04: full suite passes after deletion |

## Coverage Gaps

None. Every FR and AC in the design doc is claimed in at least one task's `implements` list and has a matching binary-verifiable Verify item.

## Contradictions

None. The task sequence (T01 → T02 → T03 → T04) is identical to the `## Architecture` 5-step lift-before-delete ordering. No Prompt or Focus statement conflicts with the design doc.

## Warnings

**AC#6 dual-claim (T03 and T04):** AC#6 is in both tasks' `implements` lists. This is intentional and correct — T03 proves the suite is green before production deletion, T04 re-proves it after. The design's "migrate callers then delete" pattern explicitly requires this split. No issue.

**FR#2 not re-verified in T04:** T04 does not include FR#2 in its Verify items. This is appropriate: FR#2 is fully owned by T02 and is protected by the T01 schema-equivalence pin (which T04 is required to keep green). No gap.

**context.md Visual Artifacts section says "None.":** The design doc contains no visual artifacts. The section is present with explicit "None." — satisfies the non-empty requirement as an intentional nil declaration.
