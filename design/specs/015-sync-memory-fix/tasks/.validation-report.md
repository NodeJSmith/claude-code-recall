## Verdict: PASS

> PASS — all 18 FRs and 12 ACs are covered by a task with a matching Verify item, no contradictions found between task Prompt/Focus sections and the design doc, no non-local Verify criteria, and context.md has all five required sections with non-empty content plus a valid optional sixth section. A small number of non-blocking warnings are noted below.

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T02 | "`_prepare_exchange_data` produces the same `content_hash` for the same raw text regardless of `cap_limit` value" |
| FR#2 | T02 | "`cap_for_embedding(\"long text\", max_tokens=4096)` caps at 4096 tokens; without `max_tokens` it caps at 8192" |
| FR#3 | T02 | "`embed_branch_chunks` with `cap_limit=4096` produces exchange_data with `cap_tokens=4096` for truncated texts" |
| FR#4 | T02 | "`embed_branch_chunks` with default cap_limit produces exchange_data with `cap_tokens=8192` for truncated texts" |
| FR#5 | T01 | "The `chunks` table has a `cap_tokens INTEGER` column (check via `PRAGMA table_info(chunks)` in a fresh DB)" |
| FR#6 | T03 | "`_diff_exchanges` flags a chunk with `cap_tokens=4096` for re-embedding when `target_cap=MODEL_TOKEN_LIMIT`, even with matching `content_hash`" |
| FR#7 | T04 | "With draft-quality chunks in DB and no schedule marker, `proactive_alert_block` includes `ALERT_DRAFT_QUALITY_VECTORS` in its output" |
| FR#8 | T04 | "With a schedule marker containing `configured_at`, the alert does not fire; with `dismissed_at`, the alert does not fire; with empty `{}`, the alert fires" |
| FR#9 | T05 | "`all_entries` is deleted before the `sync_branch` loop in `sync_session` (verifiable by inspection or reference-count test)" |
| FR#10 | T05 | "`reclaim_memory()` is called between the three phases of `sync_branch` (verifiable by mocking/patching `reclaim_memory` and checking call count)" |
| FR#11 | T05 | "`embed_batch` emits DEBUG log with `batch_size` and `longest_tokens` before each `model.embed()` call; `embed_branch_chunks` emits WARNING when `was_capped=True`" |
| FR#12 | T05 | "`sync_current.run()` emits INFO log with `file_size` and `duration_s` after sync" |
| FR#13 | T02 | "`_plan_embed_batches` with `max_token_cap=4096` produces batches of at most 1 text when texts are near 4096 tokens" |
| FR#14 | T03 | "`_should_stamp_watermark` returns `False` when any chunk (existing or freshly-embedded) has `cap_tokens` non-NULL and `< MODEL_TOKEN_LIMIT`" |
| FR#15 | T03 | "`build_selection` SQL includes branches with chunks where `cap_tokens < MODEL_TOKEN_LIMIT`" |
| FR#16 | T04 | "`branch_embedding_coverage` returns correct counts for full, draft, and not-embedded branches; `compute_caveat` produces appropriate prose for each state" |
| FR#17 | T04 | "`ccrecall backfill embeddings --dismiss` writes a `dismissed_at` field to the schedule marker" |
| FR#18 | T04 | "`ccrecall backfill schedule write` creates the marker; `clear` removes it; `status` reports correct state" |
| AC#1 | T02 | "Two texts differing only past 4096 tokens produce different `content_hash` values; same raw text with different caps produces the same hash" |
| AC#2 | T02 | "`embed_branch_chunks` with `cap_limit=4096` caps at 4096; with `MODEL_TOKEN_LIMIT` caps at 8192" |
| AC#3 | T03 | "Chunk at cap_tokens=4096, backfill-context `_diff_exchanges` (target_cap=8192) flags it; after re-embed at 8192, sync-context `_diff_exchanges` (target_cap=4096) does NOT flag it" |
| AC#4 | T06 | "Embed at 4096 → backfill upgrades to 8192 → sync at 4096 again → chunk NOT re-embedded (returns 0)" |
| AC#5 | T04 | "Insert chunks with cap_tokens=4096 → alert fires. Write schedule marker → alert silent. Remove marker → alert fires again" |
| AC#6 | T05 | "`sync_session` does not hold a reference to the parsed JSONL list during `sync_branch`" |
| AC#7 | T05 | "After a sync, log output contains INFO with `file_size` and DEBUG with batch metrics" |
| AC#8 | T02 | "`_plan_embed_batches(max_token_cap=4096)` limits batch size to 1 for near-4096-token texts" |
| AC#9 | T03 | "Branch with cap_tokens=4096 chunks → watermark NOT stamped, branch selected by `build_selection`; after backfill re-embeds at 8192 → watermark stamped, branch no longer selected" |
| AC#9 | T06 | "Sync at 4096 → watermark NOT stamped, build_selection includes branch → backfill upgrades → watermark stamped, build_selection excludes branch" |
| AC#10 | T03 | "Branch with freshly-embedded chunks all having `was_capped=False` (cap_tokens=NULL) → watermark IS stamped" |
| AC#11 | T01 | "`effective_cap_tokens(None)` returns `MODEL_TOKEN_LIMIT`; `effective_cap_tokens(4096)` returns `4096`; no `TypeError` on `None` input" |
| AC#12 | T04 | "Branch with only draft-quality chunks shows \"draft quality\" in `compute_caveat`, not \"not embedded\"" |

Total FRs in design.md: 18 (FR#1–FR#18), all covered.
Total ACs in design.md: 12 (AC#1–AC#12), all covered. AC#9 is deliberately double-covered — T03 exercises it at the function level (`_should_stamp_watermark`, `build_selection` query directly), T06 exercises the same behavior as an assembled end-to-end lifecycle test, which satisfies the Operational Lifecycle Coverage requirement that isolated one-transition tests are not sufficient on their own.

## Coverage Gaps

None.

## Contradictions

None. Checked each task's Prompt/Focus against the design.md sections it claims to implement:
- T01 (schema/config/constants) matches Architecture § Cap-tokens tracking, § Config setting, and the v8 migration spec.
- T02 (hash fix, cap parameterization, attention budget) matches Architecture § Content hash fix, § Sync-path token cap, § Sync-path attention budget, including the char-vs-token-budget nuance called out in Edge Cases.
- T03 (upgrade detection/watermark/selection) matches Architecture § Backfill branch selection and watermark, correctly instructing use of the per-exchange `cap_tokens` (not the raw caller cap) in the freshly-embedded shortcut, per the design's explicit warning.
- T04 (alert/schedule/CLI/caveat) matches Architecture § Draft-quality alert, § Schedule marker, and FR#16's three-state distinction; correctly reuses `FULL_QUALITY_TOKEN_LIMIT` from `health.py` rather than importing from `embeddings.py`, preserving invariant 3.
- T05 (memory cleanup/logging) matches Architecture § Memory cleanup and § Hot-path logging, including the `messages` vs `all_entries` retention rationale.
- T06 (integration tests) matches the Test Strategy's AC#4 integration-test requirement and the Operational Lifecycle's convergence description.

## Non-Local Criteria

None. All Verify items are checkable via pytest, direct DB inspection (`PRAGMA table_info`, SQL queries), log-file inspection, or CLI invocation in the local repo.

## Warnings

- T01 adds `tests/test_config.py` to its Target Files (testing that `sync_path_token_limit` is in `DEFAULT_SETTINGS`), but `tests/test_config.py` is not listed in design.md's "Changed Files" Impact section. Minor — the design's Impact list is not exhaustive of every test file, and the addition is squarely in scope for FR#5's config requirement, but noting the discrepancy for completeness.
- `tests/test_integration.py` (T06's sole Target File for new tests) is referenced in design.md's Test Strategy ("AC#4 — no-ping-pong integration test: new test in `test_integration.py`") but is likewise absent from the "Changed Files" Impact section list. Same minor completeness note as above — this is a gap in the design doc's own Impact section, not a task/plan defect, since Test Strategy explicitly calls out the file.
- FR#9's Verify criterion in T05 ("verifiable by inspection or reference-count test") and AC#6 mirror hedged language ("by inspection or...") taken verbatim from design.md's own AC#6 wording. Not flagged as vague since it is the design doc's own acceptance-criterion phrasing, not a task-introduced weakening, but note that "by inspection" alone is not independently re-runnable — an executor should prefer the reference-count/patching approach where feasible.

None further.
