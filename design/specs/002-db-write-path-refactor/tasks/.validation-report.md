## Status: APPROVED

## Traceability Matrix

| Identifier | Task | Verify Criterion |
|------------|------|-----------------|
| FR#1 | T03 | "for an old-schema starting DB, `migrate_columns` produces identical resulting schema, `user_version`, and rows as before (T01 pins green)" |
| FR#2 | T03 | "`migrate_columns` is still idempotent — second call is a no-op, `user_version` stable (T01 idempotence pin green)" |
| FR#3 | T02 | "`migrate_db` returns the same boolean and nuke/no-op decision per starting schema (pre-v3 → True+recreate, fresh → False, already-v3 → False), and its recreate path sets `foreign_keys=ON` via `apply_base_pragmas`" |
| FR#4 | T04 | "`sync_session` returns the same integer (new-count / `0` / `-1`) and writes the same rows to `sessions`/`messages`/`branches`/`branch_messages`/`import_log` as before (T01 pin + existing suite green)" |
| FR#5 | T04 | "embed-on-write ordering preserved — vec0 upsert before version-column write, only for active leaves with a successful summary and queryable vec table" |
| FR#6 | T03 | "each version gate v1–v6 is reachable through a named helper invoked uniformly from `migrate_columns`; v1/v2/v4 are extracted helpers like v3/v5/v6, and no `PRAGMA user_version` remains in `migrate_columns`" |
| FR#7 | T02 | "`migrate_db`, `nuke_old_db`, and `recreate_fresh_schema` are each ≤50 lines (or `migrate_db` is a pure orchestrator over the two helpers)"; also AC#5 via T02/T03/T04 |
| FR#7 | T03 | "every function in `migrations.py` touched here is ≤50 lines except `migrate_columns` (pure orchestrator) and the batch loops whose per-row body is a single named helper" |
| FR#7 | T04 | "every function in `session_ops.py` is ≤50 lines except `sync_session` (pure orchestrator) and `sync_branch` if it remains a per-branch orchestrator over named helpers" |
| FR#8 | T02 | "`migrate_db` and `migrate_columns` remain importable from `ccrecall.migrations` with unchanged signatures; `apply_base_pragmas` is importable from `ccrecall.models` and still callable from `db.py`" |
| FR#8 | T03 | "`migrate_columns`, `_backfill_origin`, `_migrate_v5`, `_migrate_v6`, `_migrate_project_paths` remain importable with unchanged signatures" |
| FR#8 | T04 | "`sync_session` remains importable with unchanged signature (incl. `_project_id`)" |
| AC#1 | T01 | "`tests/test_migrations.py` exists and pins `migrate_columns` resulting `table_info` sets, `user_version`, and the v1/v2/v3/v4/v5/v6 row transformations for old-schema starting states; passes on current `src/`" |
| AC#2 | T01 | "a `migrate_columns`-run-twice test asserts the second call leaves schema and `user_version` unchanged; passes on current `src/`" |
| AC#3 | T02 | "`TestMigrateDb` asserts the reconnected DB has `foreign_keys=ON`; the assertion fails on pre-task `src/` and passes after" |
| AC#4 | T01 | "a `sync_session` DB-state pin covers the NULL-hash-stale `import_log` update path and the exact-hash `-1` skip; existing `test_session_ops.py`/`test_import_pipeline.py`/`test_sync_hook.py` still pass unchanged" |
| AC#5 | T02 | "no function added/touched in this task exceeds the line guideline except a pure orchestrator" |
| AC#5 | T03 | "a scan confirms no function in `migrations.py` exceeds the guideline except the documented orchestrators/batch-loops" |
| AC#5 | T04 | "a scan confirms no function in `session_ops.py` exceeds the guideline except documented orchestrators" |
| AC#6 | T02 | "`uv run pytest -q` passes with zero failures" |
| AC#6 | T03 | "`uv run pytest -q` passes with zero failures" |
| AC#6 | T04 | "`uv run pytest -q` passes with zero failures" |
| AC#7 | T02 | "importing `migrate_db`, `migrate_columns` from `ccrecall.migrations` and `apply_base_pragmas` from `ccrecall.models` succeeds" |
| AC#7 | T03 | "importing each public/preserved name from `ccrecall.migrations` succeeds" |
| AC#7 | T04 | "importing `sync_session` from `ccrecall.session_ops` succeeds with unchanged signature" |

## Coverage Gaps

None.

## Contradictions

None.

## Warnings

1. **AC#5 not in T01 implements list.** T01 touches `tests/test_migrations.py` (create), `tests/test_db.py`, and `tests/test_session_ops.py`. AC#5 is a scan of *function line counts in `src/`* — not a test artifact, so it is correctly not claimed by T01. No issue.

2. **FR#7 claimed across three tasks (T02, T03, T04).** This is expected for a cross-cutting line-count requirement. Each task's Verify item scopes it to the files that task touches (`migrate_db`/helpers, `migrations.py`, `session_ops.py` respectively). The cross-task coverage is additive and non-contradictory. No issue.

3. **AC#6 (full suite green) claimed by T02, T03, and T04 but not T01.** T01's Verify instructs running the full suite (`uv run pytest -q`) but does not formally claim AC#6 in its `implements` list — it claims AC#1, AC#2, AC#4. The Prompt section of T01 does specify the exact full-suite run command and requires it to pass, so the behavior is covered even without the formal `implements` claim. The omission is a minor inconsistency: AC#6 in the design doc says "all" requirements, which logically includes T01's test-only changes. This is a Warning (does not block approval since the Verify text in T01 is concrete and the full suite is required to pass).

4. **FR#8 covers `_backfill_origin` importability (T03 Verify) but this name is not mentioned in T03's `implements`.** The `implements` list for T03 includes FR#8 broadly, and the Verify item for FR#8 in T03 explicitly mentions `_backfill_origin`. No gap — the claim is present and the Verify criterion is concrete.

5. **`_migrate_v5` / `_migrate_v6` / `_migrate_project_paths` per-row helper extractions (`recompute_branch_v5`, `fix_project_path`) are implementation details described in T03's Prompt but not individually listed in Verify.** The T03 FR#7 and AC#5 Verify items require ≤50 lines on every function in `migrations.py`, which implicitly requires these extractions to succeed. The line-count scan criterion is concrete and observable. Acceptable — no vagueness flag needed.

6. **`context.md` — `## Visual Artifacts` section is present but contains only "None."** The schema requires the section to be "non-empty." A section whose sole content is "None." satisfies the non-empty check for sections where no artifacts exist (the section is explicitly present and self-documenting). Treated as satisfying the requirement.
