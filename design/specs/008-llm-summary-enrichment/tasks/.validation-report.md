# Validation Report: 008-llm-summary-enrichment

## 1. Scope Reviewed

- Reviewed `design.md`, `tasks/context.md`, and `T01` through `T08` under `design/specs/008-llm-summary-enrichment/`.
- Validation focus: FR#1-FR#18 and AC#1-AC#11 mapping, concrete local verification hooks, dependency sequencing, scope containment, contradictions, and the intentional manual handling for AC#9.

## 2. FR/AC Traceability

### Functional Requirements

- FR#1: Covered by `T02`, `T03`, and `T07` through deterministic fallback, stale/failed hide rules, and unchanged context/search behavior checks.
- FR#2: Covered by `T01` and `T05` through separate schema storage and worker-write boundaries.
- FR#3: Covered by `T02` through strict response-body validation and worker-owned envelope metadata.
- FR#4: Covered by `T02` and `T07` through current-version, `ok`, source-hash-matched render gating.
- FR#5: Covered by `T06` through canonical manual CLI wiring, selectors, `--force`, and `--check-capability` delegation.
- FR#6: Covered by `T06` through detached post-sync spawn, best-effort failure handling, and hook-envelope preservation.
- FR#7: Covered by `T04` through argv-list subprocess invocation and no-shell checks.
- FR#8: Covered by `T04` through safe mode, empty strict MCP config, Read-only tools, capability sidecar, and no-session-persistence checks.
- FR#9: Covered by `T05` through explicit transaction-boundary checks around Claude invocation.
- FR#10: Covered by `T05` through status classification, retry policy, and deterministic-preservation checks.
- FR#11: Covered by `T06` and `T08` through CLI/help/README disclosure of opt-in data flow and prerequisites.
- FR#12: Covered by `T03` and `T05` through source-hash creation/invalidation and compare-and-swap stale-write suppression.
- FR#13: Covered by `T04` through packet-only `--add-dir`, isolated `cwd`, and validated source-resolution checks.
- FR#14: Covered by `T04` through active-branch-only packet coverage, duplicate handling, and complete UUID coverage checks.
- FR#15: Covered by `T02` and `T04` through renderer priorities, prompt contract, and transcript-packet evidence preservation.
- FR#16: Covered by `T02` and `T04` through UUID-allowlist validation and prompt/provenance requirements.
- FR#17: Covered by `T02` and `T07` through primary/supplementary budgets and deterministic-context retention.
- FR#18: Covered by `T01`, `T04`, `T06`, and `T08` through settings defaults, unchanged invocation pass-through, and user-facing budget documentation.

### Acceptance Criteria

- AC#1: Covered by `T03`, `T06`, and `T07` through deterministic regression checks across sync, hook, context, and search paths.
- AC#2: Covered by `T02` and `T07` through valid composition and deterministic-only fallback tests.
- AC#3: Covered by `T02` through malformed-output, unknown-field, citation, and worker-metadata rejection tests.
- AC#4: Covered by `T04` and `T05` through subprocess failure classification and worker-level diagnostic handling.
- AC#5: Covered by `T01` through fresh install, upgrade, and idempotent migration coverage.
- AC#6: Covered by `T06` through hook stdout, import-boundary, and spawn-only checks.
- AC#7: Covered by `T06` through direct-worker lightweight startup and no-vec/no-embedding-path checks.
- AC#8: Covered by `T07` through additive search display preference with unchanged retrieval order and deterministic `topic` retention.
- AC#9: Covered by `T08` as an explicitly manual acceptance path with corpus, manifest, procedure, and results record.
- AC#10: Covered by `T02` and `T07` through render-budget and continuation-hint-priority checks.
- AC#11: Covered by `T06` and `T08` through invocation/default-budget checks and threshold-semantics documentation.

Assessment: every FR and AC is mapped to at least one task, and each mapped task includes concrete Verify clauses that can be checked locally.

## 3. Prompt-To-Verify Coverage And Local Checks

- `T01`: Prompt, Focus, and Verify consistently cover additive schema changes, rebuild-safe migrations, lightweight DB access, and six-setting config defaults/overrides.
- `T02`: Prompt, Focus, and Verify consistently cover strict schema validation, worker-owned envelope fields, canonical source hashing, freshness gating, and budgeted rendering.
- `T03`: Prompt, Focus, and Verify consistently cover `summary_source_hash` creation, invalidation, NULL-on-failure behavior, and deterministic no-regression safeguards.
- `T04`: Prompt, Focus, and Verify consistently cover source resolution, secure packet construction, isolated invocation, capability-sidecar behavior, and conservative failure classification.
- `T05`: Prompt, Focus, and Verify consistently cover eligibility, PID exclusion, no-open-write-transaction Claude calls, compare-and-swap writes, and stale-result suppression.
- `T06`: Prompt, Focus, and Verify consistently cover canonical CLI wiring, config-gated detached spawn, hook stdout invariants, lightweight direct-worker startup, and privacy/budget disclosures.
- `T07`: Prompt, Focus, and Verify consistently cover hot-path row shaping, validity-gated context composition, additive search fields, and unchanged ranking/fallback behavior.
- `T08`: Prompt, Focus, and Verify consistently cover README disclosure, de-identified evaluation corpus, manifest integrity, evaluator procedure, and committed manual review record.

Assessment: task prompts are concrete, and the Verify sections define actionable local checks rather than vague completion criteria.

## 4. Dependency Sequencing And Scope

- `T01` is correctly foundational for schema, migrations, config, and the lightweight DB boundary.
- `T02` is correctly independent and foundational for shared validation, rendering, and source-hash helpers.
- `T03` now correctly depends on both `T01` and `T02`, which resolves the prior schema-ordering gap.
- `T04` correctly depends on `T01` and `T02` for settings/schema plus shared response schema and status helpers.
- `T05` correctly depends on `T01` through `T04`, since the worker needs schema, hashing, validation/render helpers, and the Claude boundary.
- `T06` correctly depends on `T05`, because the CLI and detached spawn are thin entry points over the finished worker/capability boundary.
- `T07` correctly depends on `T01` and `T02`; it only needs the stored columns and lightweight validity/render helpers, not the worker implementation.
- `T08` correctly depends on `T06` and `T07`, which transitively brings in the completed worker path before the manual evaluation procedure is executed.
- Scope remains contained: no task expands into embeddings, ranking changes, Agent SDK adoption, `context_summary_json` mutation, or automatic historical SessionStart backfill.

Assessment: dependency sequencing is now sound, and task scope matches the design's declared goals and non-goals.

## 5. Contradictions And Manual Criteria

- No material contradiction remains between `design.md`, `tasks/context.md`, and the task files.
- The six LLM settings are described consistently across design and tasks.
- The privacy contract is consistently framed as explicit opt-in plus capability gate, with deterministic fallback preserved.
- Search presentation remains additive and ranking-neutral everywhere it is described.
- AC#9 is intentionally manual, and the plan is valid because `T08` includes the required corpus artifacts, manifest, evaluator procedure, and committed `evaluation-results.md` record.

Assessment: no blocking contradictions remain, and AC#9 is properly planned as a manual acceptance gate rather than missing automation.

## 6. Verdict

Verdict: **Pass**.

Blockers: **None**.
